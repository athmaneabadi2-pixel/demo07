services:
- type: web
  name: companion-demo07
  env: python
  plan: free
  buildCommand: pip install -r requirements.txt
  startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
  envVars:
  - key: PORT
    value: 8000
  healthCheckPath: /health

